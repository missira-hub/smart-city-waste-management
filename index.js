import React from "react";  
import { registerRootComponent } from "expo";
import App from "./App";

registerRootComponent(App);

// The entry point for the app. This file is used to register the root component of the app with Expo.